{% set cpu_topology = ansible_local["topology"]["cpu_topology"] -%}
{% set gpu_topology = ansible_local["topology"]["gpu_topology"] -%}
{% for affinity in gpu_topology %}
Name=gpu Type={{ gpu_type }} File=/dev/nvidia{{ loop.index0 }} COREs={{ affinity }}
{% endfor %}